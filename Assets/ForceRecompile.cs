// Script temporal para forzar recompilación de Unity
// Este archivo solo existe para forzar a Unity a recompilar todos los scripts
// Puedes eliminarlo después de que Unity compile correctamente

using UnityEngine;

public class ForceRecompile : MonoBehaviour
{
    // Este script no hace nada, solo fuerza recompilación
}
